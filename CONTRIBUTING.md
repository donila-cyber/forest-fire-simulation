# Руководство для разработчиков

## 📌 Правила работы с ветками

### Типы веток:
- `feature/` - новая функция
- `bugfix/` - исправление ошибки
- `docs/` - документация
- `test/` - тесты

### Примеры:
- `feature/add-gui`
- `bugfix/fix-temperature-calculation`
- `docs/update-readme`

## 🔄 Процедура Code Review

1.  **Создание Pull Request (PR)**: После завершения задачи и локального тестирования, разработчик создает Pull Request в основную ветку (`main`).
2.  **Назначение ревьюера**: Team Lead или другой назначенный участник команды назначается ревьюером.
3.  **Проверка кода**: Ревьюер проверяет код на соответствие стандартам кодирования, функциональность, наличие тестов и общую архитектуру.
4.  **Комментарии и исправления**: Все замечания и предложения оставляются в комментариях к PR. Разработчик вносит необходимые исправления.
5.  **Утверждение и слияние**: После устранения всех замечаний и одобрения ревьюером, PR сливается в основную ветку.

## 📝 Правила кода (Python)

### Именование:
- Функции: `расчет_температуры`
- Классы: `МодельПожара`
- Константы: `МАКС_ТЕМПЕРАТУРА`

### Комментарии:
```python
def расчет_распространения(ветер, температура):
    """
    Рассчитывает скорость распространения пожара.
    
    Параметры:
    ветер - скорость ветра в м/с
    температура - текущая температура
    
    Возвращает:
    скорость распространения
    """
    # Формула расчета
    скорость = температура * 0.1 + ветер * 0.2
    return скорость
